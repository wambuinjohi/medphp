# ============================================================================
# FRONTEND ENVIRONMENT VARIABLES (.env)
# ============================================================================
# Copy this file to .env and update with your values
# Frontend is built with Vite, so use VITE_ prefix for environment variables

# API Base URL - where the PHP backend is hosted
# If running locally: http://localhost:8000
# If remote: https://med.wayrus.co.ke
VITE_API_URL=https://med.wayrus.co.ke

# Backend PHP API URL (for external API calls)
VITE_EXTERNAL_API_URL=https://med.wayrus.co.ke/api.php

# ============================================================================
# BACKEND ENVIRONMENT VARIABLES
# ============================================================================
# These go on your server in /path/to/app/.env or server environment

# Database Configuration
DB_HOST=localhost
DB_USER=wayrusc1_med
DB_PASS=your_password_here
DB_NAME=wayrusc1_med

# JWT Configuration
JWT_SECRET=your_jwt_secret_here

# ============================================================================
# KRA eTIMS INTEGRATION CONFIGURATION
# ============================================================================
# For sandbox testing first, then switch to production

# Environment: sandbox or production
ETIMS_ENV=sandbox

# Sandbox Credentials (Testing with KRA)
ETIMS_SANDBOX_URL=https://sandbox.etims.kra.go.ke/api/submit-sale
ETIMS_SANDBOX_TIN=12345678
ETIMS_SANDBOX_BHF_ID=001
ETIMS_SANDBOX_VSCU_ID=001
ETIMS_SANDBOX_API_KEY=sandbox-key-here

# Production Credentials (Use after sandbox approval)
ETIMS_PRODUCTION_URL=https://etims.kra.go.ke/api/submit-sale
ETIMS_PRODUCTION_TIN=12345678
ETIMS_PRODUCTION_BHF_ID=001
ETIMS_PRODUCTION_VSCU_ID=001
ETIMS_PRODUCTION_API_KEY=

# eTIMS Retry Configuration
ETIMS_MAX_RETRIES=5
ETIMS_RETRY_DELAY_MINUTES=15
ETIMS_REQUEST_TIMEOUT_SECONDS=30

# eTIMS Feature Flags
ETIMS_AUTO_RETRY=true
ETIMS_ENABLED=true

# ============================================================================
# SETUP INSTRUCTIONS
# ============================================================================
#
# 1. FRONTEND SETUP
#    - Create .env in your project root
#    - Add VITE_API_URL pointing to your backend
#    - Example: VITE_API_URL=https://med.wayrus.co.ke
#
# 2. BACKEND SETUP (on server)
#    - Create .env in your PHP app directory
#    - Add all DB and eTIMS variables
#    - Variables are loaded via getenv() in PHP
#    - Make sure .env is NOT in version control
#
# 3. ENABLE eTIMS
#    - Set ETIMS_ENV=sandbox for testing
#    - Get sandbox credentials from KRA
#    - Add credentials to ETIMS_SANDBOX_* variables
#    - After sandbox approval, add ETIMS_PRODUCTION_* variables
#    - Switch ETIMS_ENV=production when ready for live
#
# 4. VERIFY SETUP
#    - Frontend: /api?action=etims_status should work
#    - Backend: Check that EtimsService.php is loaded
#    - Database: Verify eTIMS tables exist (etims_sales, etc.)
