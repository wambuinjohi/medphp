# Backend API Configuration Example
# Copy this file to .env and fill in your values

# Database Configuration (REQUIRED)
# These must be set for the API to work
DB_HOST=localhost
DB_USER=your_db_user
DB_PASS=your_db_password
DB_NAME=your_database_name

# JWT Configuration (REQUIRED)
# This is used to sign and verify JWT tokens
# Generate a secure random string (at least 32 characters)
JWT_SECRET=your-secure-secret-key-here-minimum-32-chars

# File Upload Configuration (Optional)
# Directory where uploaded files will be stored
# Default: /public/uploads (relative to project root)
UPLOADS_DIR=/path/to/uploads

# API Server Configuration (Optional)
# Used for generating file URLs after upload
# These are typically auto-detected from the request
# API_HOST=https://med.layonsconstruction.com
# API_PROTOCOL=https

# Debugging (Optional)
# Set to 1 to enable debug logging
# DEBUG=0

# CORS Configuration (Optional)
# Whitelist specific origins (commented out by default for flexibility)
# CORS_ALLOWED_ORIGINS=https://med.layonsconstruction.com,http://localhost:3000

# Production Checklist:
# - Set DB_HOST, DB_USER, DB_PASS, DB_NAME to your production database
# - Generate a secure JWT_SECRET (use: openssl rand -base64 32)
# - Set UPLOADS_DIR to a writable directory
# - Ensure database user has appropriate permissions
# - Configure CORS_ALLOWED_ORIGINS for production domain
# - Review security settings in the API code
